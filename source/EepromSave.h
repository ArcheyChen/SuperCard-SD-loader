#pragma once
#include "Save.h"

bool eeprom_patchV111(const struct save_type_t* type);
bool eeprom_patchV120(const struct save_type_t* type);
bool eeprom_patchV124(const struct save_type_t* type);
bool eeprom_patchV126(const struct save_type_t* type);